import { makeStyles } from "@material-ui/core/styles";

export const PerfilStyles = makeStyles((theme) => ({
  title: {
    marginTop: 15,
  },

  contenedor: {
    position: "relative",
    top: 30,
  },

  contenedorPeque: {
    background: "red",
  },
}));
